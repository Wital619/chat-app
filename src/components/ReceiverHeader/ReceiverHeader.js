import React from 'react';

import styles from './ReceiverHeader.scss';

const ReceiverHeader = () => (
  <header className={styles.receiverHeader}>
    <h1 className={styles.userName}>Vitaliy Pogoretskyy</h1>
  </header>
);

export default ReceiverHeader;